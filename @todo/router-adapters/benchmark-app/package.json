{
  "name": "benchmark-app",
  "version": "0.0.0",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "concurrently -k -n server,vite -c blue,green \"npm:server\" \"npm:client\"",
    "client": "vite",
    "server": "node --watch server/index.js",
    "build": "vite build",
    "preview": "vite preview"
  },
  "devDependencies": {
    "concurrently": "^10.0.3",
    "vite": "^8.0.12"
  },
  "dependencies": {
    "dotenv": "^17.4.2",
    "lowdb": "^7.0.1"
  }
}
